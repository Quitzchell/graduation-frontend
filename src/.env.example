NEXT_PRIVATE_DEBUG=true
NEXT_PUBLIC_DEBUG=true

# Backend API URL (internal Docker network)
NEXT_PUBLIC_BACKEND_URL=http://backend-container-name:80

# Public API URL (browser accessible)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Asset URL (browser accessible)
ASSETS_URL=localhost:8080

# Redis configuration
REDIS_HOST=host.docker.internal
REDIS_PORT=6379
REDIS_SCHEME=tcp